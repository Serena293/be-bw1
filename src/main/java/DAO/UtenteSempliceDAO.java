package DAO;
/*
import Entities.UtenteSemplice;
import jakarta.persistence.EntityManager;
import jakarta.persistence.EntityTransaction;

public class UtenteSempliceDAO {
  private EntityManager em;

  public UtenteSempliceDAO(EntityManager em){
      this.em = em;
  }

  public void saveUtenteSemplice(UtenteSemplice utenteSemplice){
      EntityTransaction et = em.getTransaction();
      et.begin();
      em.persist(utenteSemplice);
      et.commit();
  }

  public UtenteSemplice findUtenteSempliceById(Long id){
      return em.find(UtenteSemplice.class, id);
  }

  public void updateUtenteSemplice (UtenteSemplice utenteSemplice){
      EntityTransaction et = em.getTransaction();
      et.begin();
      em.merge(utenteSemplice);
      et.commit();
  }
}
*/